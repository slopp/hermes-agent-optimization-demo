[project]
name = "hermes-agent-optimization-demo"
version = "0.1.0"
description = "Deterministic enterprise MCP environment for Hermes harness optimization."
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
ignore = ["UP017"]
