# Override these paths with absolute paths for your run.
trace:
  filesystem:
    path: .runs/world-v2/baseline-dev/insights.jsonl
output:
  directory: .runs/world-v2/baseline-dev/insights-out
evidence_streams:
  anomaly_and_patterns:
    input_scaling: robust
    minimum_independent_traces: 3
  tool_issues:
    minimum_independent_cases: 3
analyst:
  enabled: false
